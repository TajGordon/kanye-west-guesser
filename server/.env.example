# Environment Configuration
NODE_ENV=development

# Server Configuration
# When using nginx reverse proxy (RECOMMENDED):
#   - Set PORT to 3000 (or any port)
#   - Set HOST to localhost (not exposed to internet)
#   - nginx listens on 80/443 and proxies to localhost:3000
# When running standalone (NOT RECOMMENDED for production):
#   - Set HOST to 0.0.0.0 to accept external connections
PORT=3000
HOST=localhost

# CORS Configuration
# IMPORTANT: This should match your FRONTEND domain, not your server domain
# 
# Development (client runs on localhost:5173):
CORS_ORIGIN=http://localhost:5173
#
# Production with nginx (frontend served from same domain):
# CORS_ORIGIN=https://yourdomain.com
#
# Testing with public IP (before domain setup):
# CORS_ORIGIN=http://123.45.67.89
#
# Production with separate frontend domain:
# CORS_ORIGIN=https://app.yourdomain.com
#
# Multiple origins (comma-separated):
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com,https://app.yourdomain.com

# Socket.IO Configuration
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000

# Game Configuration
MAX_LOBBIES=100
LOBBY_DESTROY_GRACE_MS=60000
LOBBY_CLEANUP_INTERVAL_MS=30000

# Question Data
QUESTIONS_DATA_PATH=./data/questions

# Session Configuration (if needed later)
SESSION_SECRET=change-this-in-production-to-a-random-string

# Logging
LOG_LEVEL=info
